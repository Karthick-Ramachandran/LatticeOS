{
  "name": "@latticeos/cli",
  "version": "0.1.0-rc.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "files": [
    "dist"
  ],
  "bin": {
    "lattice": "./dist/bin.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "prebuild": "pnpm --filter @latticeos/analyzer build",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test:compile": "tsc -p tsconfig.test.json",
    "pretest": "pnpm --filter @latticeos/analyzer build",
    "test": "pnpm test:compile && node --test .test-dist/*.test.js",
    "pretest:package": "pnpm -r --filter @latticeos/cli... build",
    "test:package:workspace": "node scripts/test-packed-consumer.mjs",
    "test:npm-package": "node --test scripts/npm-package.test.mjs",
    "test:package": "node scripts/test-packed-consumer.mjs && pnpm test:npm-package && node scripts/test-npm-consumer.mjs",
    "release:pack": "node scripts/release-pack.mjs"
  },
  "dependencies": {
    "@latticeos/analyzer": "workspace:*",
    "@latticeos/core": "workspace:*"
  }
}
