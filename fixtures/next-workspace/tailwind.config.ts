throw new Error("LatticeOS must never execute consumer Tailwind configuration");

export default {
  theme: {
    extend: {
      colors: {
        accent: "#7c3aed",
      },
      spacing: {
        card: "1.25rem",
      },
    },
  },
};
